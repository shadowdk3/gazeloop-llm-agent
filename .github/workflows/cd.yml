name: CD

on:
  push:
    branches: [main]

jobs:
  deploy:
    needs: test
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4

      - name: Build Docker image
        run: docker build -t gazeloop-llm-agent:${{ github.sha }} .

      # Push image to registry
      # Deploy to server